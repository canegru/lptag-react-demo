Quick example on how to load lpTag.newPage within a react app.
Project generated with [Create React App].

## Available Scripts

In the project directory, you can run:

### `npm start`

Install

### `npm i`
### `npm install`
